VITE_CONTACT_ENDPOINT="http://localhost:8000/server/contact.php"
# In local development you can run a local PHP server that serves /server/contact.php
# or point to the staging endpoint. Keep RECAPTCHA empty for CLI local tests.
VITE_RECAPTCHA_SITE_KEY=""
